# Simple stopwatch app

A simple stopwatch built with React using hooks (useState, useEffect, and useRef).

It supports starting, stopping, and resetting the timer with accurate millisecond precision.
